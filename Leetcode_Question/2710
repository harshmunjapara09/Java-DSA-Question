class Solution {
    public String removeTrailingZeros(String num) {
        int n = num.length();
        int j = n-1;
        while (num.charAt(j)=='0'){
            j--;
        }
        return num.substring(0,j+1);
    }
}