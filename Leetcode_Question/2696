class Solution {
    public int minLength(String s) {
         int n=s.length();
        for (int i = 0; i < n; i++) {
            s = s.replaceAll("AB","");
            s = s.replaceAll("CD","");
        }
        return s.length();
    }
}